[pytest]
asyncio_mode = auto
# HA flow tests only. Core tests must run separately with the HA plugin off
# (it blocks sockets and replaces the event loop):
#   python -m pytest tests/test_core.py -p no:homeassistant
testpaths = tests/ha
